# Use a Python image optimized for size
FROM python:3.10-slim

# Set environment variables:
# 1. PYTHONDONTWRITEBYTECODE: prevents Python from writing .pyc files (saves space)
# 2. PYTHONUNBUFFERED: ensures output is immediately streamed to Docker logs
ENV PYTHONDONTWRITEBYTECODE 1
ENV PYTHONUNBUFFERED 1

# Set the working directory
WORKDIR /app

# Create a simple Python script
RUN echo "import datetime; print(f'Python time in container: {datetime.datetime.now()}')" > script.py

# Define the command to execute the script
CMD ["python", "script.py"]

